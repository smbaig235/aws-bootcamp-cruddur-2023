#!/usr/bin/env python3.8

import boto3

attrs = {
  'endpoint_url': 'http://localhost:8000'
}
db = boto3.resource('dynamodb',**attrs)
table_name = 'cruddur-messages'

table = db.Table(table_name)
response = table.scan()

items = response['Items']
for item in items:
  print(item)